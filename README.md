# gopher-server
A simple gopher server

# TODO
- Further support for all the media types (if even needed...)

- Compile with asan/tsan

- Better gophermap creation. E.g. only add host/port if they're
  missing from the file.
